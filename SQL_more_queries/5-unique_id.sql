-- create a table called unique_id with two columns: id and name. The id column should be unique and have a default value of 1.
CREATE TABLE IF NOT EXISTS unique_id (id INT DEFAULT 1 UNIQUE, name VARCHAR(256));